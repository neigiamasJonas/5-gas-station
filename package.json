{
  "name": "5-gas-station",
  "version": "1.0.0",
  "description": "Sign up (pagal paskaitas)",
  "main": "index.js",
  "scripts": {
    "dev": "dead-server --port=3001 --host=localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "left/right column, main/form css, background-image"
  ],
  "author": "Povilas",
  "license": "ISC",
  "devDependencies": {
    "dead-server": "^1.0.6"
  }
}
